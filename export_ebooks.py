import json
import os

# The result is stored in the tool output. I will read the full result using a script
# since the previous tool output was truncated for display but the data is available.
# Actually, I can just fetch it again but writing it directly to a file.
